import React from "react";

const EditFormExam = ({ slug }: { slug: string }) => {
    return (
        <div>
            <h1>Edit Exam: {slug}</h1>
        </div>
    );
};

export default EditFormExam;
